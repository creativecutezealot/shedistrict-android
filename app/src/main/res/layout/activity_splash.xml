<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.motion.widget.MotionLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:showPaths="true"
    android:background="@color/activityBg"
    app:layoutDescription="@xml/scene_splash"
    android:id="@+id/motionLayout"
    android:orientation="vertical"
    tools:context=".view.activity.SplashActivity">


    <ImageView
        android:id="@+id/ivLogo"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/appicon"/>

    <View
        android:id="@+id/vvBase"
        android:layout_width="match_parent"
        android:layout_height="1px"/>
    <ProgressBar
        android:id="@+id/pbProgress"
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:layout_gravity="center"
        android:layout_marginTop="30dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:max="100"
        android:progressDrawable="@drawable/customprogress"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        app:layout_constraintTop_toBottomOf="@id/vvBase"/>
    <TextView
        android:id="@+id/tvText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="16dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:gravity="center"
        android:fontFamily="@font/roboto_regular"
        android:text="Patience is key! We’re a brand new app, so it’ll look a little like ghost town here. Thank you for helping us fill the community!"
        app:layout_constraintTop_toBottomOf="@id/pbProgress"/>
</androidx.constraintlayout.motion.widget.MotionLayout>

    <!--android:text="Check out the forum for cool dicussion,\nthe latest gossip and more!"-->