<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <corners
                android:radius="8dp"/>
            <gradient
                android:startColor="#DDC2CB"
                android:centerColor="#DDC2CB"
                android:centerY="1.0"
                android:endColor="#DDC2CB"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners
                    android:radius="8dp"/>
                <gradient
                    android:startColor="#D46185"
                    android:centerColor="#D46185"
                    android:centerY="1.0"
                    android:endColor="#D46185"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>